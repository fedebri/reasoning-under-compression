name: reasoning-compression
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - jupyterlab
  - ipykernel
  - pandas
  - numpy
  - pyarrow
  - scikit-learn
  - matplotlib
  - seaborn
  - tqdm
  - pytest
  - spacy
  - spacy-model-en_core_web_sm
  - mkdocs
  - mkdocs-material
  - mkdocstrings
  - mkdocstrings-python
  - pip:
      - datasets
      - huggingface-hub
      - tiktoken

# conda env create -f reasoning_env.yml
# conda activate reasoning-compression
# python -m ipykernel install --user --name reasoning-compression \
#   --display-name "Python (reasoning-compression)"
