name: reasoning-compression
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - jupyterlab
  - ipykernel
  - pandas
  - numpy
  - pyarrow
  - scikit-learn
  - matplotlib
  - seaborn
  - tqdm
  - pip:
      - datasets
      - huggingface-hub
      - tiktoken





## conda env create -f environment.yml
## conda activate reasoning-compression
## python -m ipykernel install --user --name reasoning-compression --display-name "Python (reasoning-compression)"